## File's tree:

```
├─ backend/
│  ├─ fetch.rs // Fetch and downloads video functions
|  ├─ get_directory.rs // Check and/or create a directory to download the videos in.
|  ├─ mod.rs 
|  ├─ response.json // response JSON example.

```
